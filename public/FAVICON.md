# Favicon

Place the production `favicon.ico` at `public/favicon.ico`. Next.js serves it automatically at `/favicon.ico`.

Do not commit placeholder favicons — replace with the final brand asset before launch.
